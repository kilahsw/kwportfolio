import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import Footer from './Footer';
import '../App.css';

const Meet = () => {
	
	return (
		<div>
			<p>about goes here</p>
			{/* <p onClick={() => {
                setMeetToggle(false)
            }}>Close</p> */}
		</div>
	);
};

export default Meet;
